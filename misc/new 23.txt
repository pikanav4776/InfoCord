📌 Prompt
I am designing an offline-first web application (Flask backend + JavaScript frontend) that supports local data storage, a sync queue, and eventual synchronization with a cloud database.
I need a deep technical overview of best practices for:
1)Offline-first architecture for web applications
2)Implementing a local “write queue” for unsynced user actions (create/update/delete operations)
3)Using IndexedDB or alternative browser storage for persistent offline queues
4)Network state detection in browsers (online/offline events) and their reliability limitations
5)Background synchronization strategies between local storage and a server (Flask API or REST backend)
6)Conflict resolution strategies for offline edits (e.g., last-write-wins vs merge strategies)
7)Exponential backoff strategies for retrying failed sync operations, including when to cap or stop retries
8)Battery and resource optimization considerations for background sync in web applications
9)Real-world examples of apps that use offline-first sync models (e.g., Notion, Google Docs, Trello)
Please explain architecture patterns, provide examples, and highlight tradeoffs between simplicity and robustness. Prefer practical implementation guidance over theoretical descriptions.

Please clarify
1)"blocking on network round-trips"
2)Make every mutation idempotent with an opId or request key.
3)CRDT/OT-style approaches: strongest for collaborative editing, but much more complex.
4)Use LWW for trivial fields.
5)Keep version numbers or ETags on every resource.
6)"Add randomness so many clients do not retry at the same instant." How and what kinds of randomness should I add?
7)ETag-style concurrency checks
8)Idempotency keys on every mutation
9)"IndexedDB + outbox + batched REST sync + LWW"
10)what it means to add versioning, merge rules, tombstones, idempotency, and a conflict review UI.